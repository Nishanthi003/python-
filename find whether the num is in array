#find whether the num is in array
arr=list(map(int,input("Enter the array:").split()))
n=len(arr)
a=int(input("Enter the a:"))
for i in range(n):
    if a in arr:
       print("True")
            
            
